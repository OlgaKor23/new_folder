import React, {useState} from 'react'import './Navigation.scss'import { FC } from 'react'import line_01 from '../../components/img/line-01.svg';import line_02 from '../../components/img/line-02.png';import { IAction } from "../../reducer/reducer";import { EDITIONS } from "../../consts/consts";interface INavigationProps {    className?: string;    dispatch: (action: IAction) => void;    initialOptionPos?: boolean;}export const Navigation:FC<INavigationProps> = ({    className,    dispatch,    initialOptionPos,}) => {    const [activeOptionIndex, setActiveOptionIndex] = useState(false);    const onItemClick = (edition: EDITIONS) => {        const action: IAction = {            type: edition        };        dispatch(action);    };    const setActiveIndex = (() => {        setActiveOptionIndex(activeOptionIndex);        console.log('ok')    });    const classNameActive = classNames('navigation', className)    return (        <ul className="navigation">            <img className="navigation-imgLine" src={line_01} alt=""/>            <li                className="navigation-title"                onClick={()=>{ onItemClick(EDITIONS.STANDARD)}}            >                STANDARD            </li>            <img className="navigation-imgLine" src={line_02} alt=""/>            <li                className="navigation-title"                onClick={()=>{ onItemClick(EDITIONS.DELUXE)}}            >                DELUXE            </li>            <img className="navigation-imgLine" src={line_02} alt=""/>            <li                className="navigation-title"                initialOptionPos={setActiveIndex}                onClick={()=>{ onItemClick(EDITIONS.ULTIMATE)}}            >                ULTIMATE            </li>            <img className="navigation-imgLine" src={line_01} alt=""/>        </ul>    )};