import React from 'react'import './EditionCover.scss'import { FC } from 'react'import { Button } from "../../Button/Button";import stalker from '../../../components/img/stalker.png';interface IEditionCoverProps {    className?: string;    callback?: (e: React.MouseEvent) => void}export const EditionCover:FC<IEditionCoverProps> = (className, callback) => {    const onButtonClick = (e: React.MouseEvent):void => {        if (callback) {            callback(e);        }    };    return (        <div className="editionCover">            <img className="editionCover-img" src={stalker}/>            <Button                className="editionCover-button"                callback={onButtonClick}            />        </div>    )};