import React, {useEffect, useState} from 'react';import './PreorderBonusComponent.scss';import { FC } from 'react';import radiation from '../../../../components/img/radiation.svg';import radiation_disabled from '../../../../components/img/radiation-disabled.svg';import iconInfo from '../../../../components/img/small.svg';import iconInfo_disabled from '../../../../components/img/small-disabled.svg';interface IPreorderBonusComponentProps {    text: string;    isDisabled?: boolean;    className?: string;}export const PreorderBonusComponent:FC<IPreorderBonusComponentProps> = ({text, className, isDisabled}) => {    return (       <div           className="preorderBonusComponent"       >           {!isDisabled ? (               <img                   className="preorderBonusComponent-imgRadiation"                   src={radiation}               />               ) :               <img                   className="preorderBonusComponent-imgRadiation"                   src={radiation_disabled}               />           }           {!isDisabled ? (               <span className="preorderBonusComponent-text">{text}</span>           ) :               <span className="preorderBonusComponent-text--disabled">{text}</span>           }           {!isDisabled ? (               <img                   className="preorderBonusComponent-imgInfo"                   src={iconInfo}               />           ) :               <img                   className="preorderBonusComponent-imgInfo"                   src={iconInfo_disabled}               />           }       </div>    )};