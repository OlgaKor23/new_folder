import React, {useEffect, useState} from 'react'import './EditionBonus.scss'import { FC } from 'react'import { PreorderBonusComponent } from "./PreorderBonusComponent/PreorderBonusComponent";import bonus from '../../../components/img/bonus.png';import classNames from "classnames";interface IEditionBonusProps {    isDisabled?: boolean;}export const EditionBonus:FC<IEditionBonusProps> = ({}) => {    const [isState, setIsDisabled] = useState(false);    useEffect(() => {        setIsDisabled(!isState);    }, []);    const classNamesEditionBonus = classNames( 'editionBonus',        {            'editionBonus--disabled' : !isState,        },    );    return (        <div className="editionBonus">            <img className="editionBonus-imgBonus" src={bonus} alt="bonus"/>            <div className="editionBonus-container">                <div className="editionBonus-list">                    <PreorderBonusComponent                        // classNames={classNamesEditionBonus}                        text="Preorder Bonus"                    />                    <PreorderBonusComponent text="Special Extra Quest"/>                    <PreorderBonusComponent text="Official Soundtrack"/>                    <PreorderBonusComponent text="3 Costumes, 7 Weapon skins"/>                    <PreorderBonusComponent text="SEASON PASS"/>                </div>                <div className="editionBonus-list">                    <PreorderBonusComponent text="Steam Exclusive Content"/>                    <PreorderBonusComponent text="Digital Artbook"/>                    <PreorderBonusComponent text="1 Costume, 3 Weapon skins"/>                    <PreorderBonusComponent text="2 Story Expansions"/>                </div>            </div>        </div>    )};